source 'https://rubygems.org/'

gem 'sinatra'
gem 'sinatra-contrib'

gem 'redis'
gem 'foreman'

gem 'LodViewRewrite', github: 'inohiro/LodViewRewrite'

group :test do
  gem 'rspec'
  gem 'rack-test'
end

group :production do
  gem 'newrelic_rpm'
end
